Didn't realize but swarm requires prebuilt images, docker etc
So any service that runs an api needs to compile to a docker image we can use
Cant just run the local repo

Apps arent affected since they compile to the nginx webserver anyway

