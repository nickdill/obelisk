Running into ownership conflict
if the megalisk sets each modules type, removes need for individual module configs
but can set wrong type, much easier to import the modules without having to configure anything, they have configs predone


FIXME: docker-compose.override needs to include the nginx still for local, need it for routing

