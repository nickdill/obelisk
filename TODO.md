Have this app serve a basic admin panel.
Have nginx add a /megalisk path or something.
Configure, add new modules from the UI
Deploy a blank megalisk app,
It renders the wizard web ui,
Type in git repos,
It pulls and deploys
