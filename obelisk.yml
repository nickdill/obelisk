version: "0.1"
name: "obelisk"
type: server
domains:
  local: obelisk.localhost
modules:
  agent:
    git_source: ../obelisk-agent
    domains:
      local: agent.localhost
  dev:
    git_source: ../obelisk-dev
    domains:
      local: dev.localhost
