
To consider MVP finished, Obelisk needs to be deployable to production, take in a list of modules (build images that we can now pull, apparently only private gh? tbd)

Need to verify swarm works as expected and that needed configs can be set, memory, domains, all things


Also realized the custom domains are hard over the network bc the hosts file 
